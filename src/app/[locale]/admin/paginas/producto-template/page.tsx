import { LandingEditor } from "@/components/landing-builder/LandingEditor";

export default function ProductoTemplateEditorPage() {
  return <LandingEditor pageKey="pageProductoTemplateJson" pageLabel="Producto (template)" />;
}
